# examples/

Examples and fixtures for contributor onboarding and deterministic tests.

## Contents
- `intents/`: sample intent definitions.
- `topology/`: sample topology snapshots.
- `state/`: observed state snapshots for reconcile/drift exercises.
- `rendered-configs/`: compiled artifact fixtures.

Keep examples aligned with OpenAPI payloads and implemented handlers.

## TODO
- TODO(truthwatcher): add end-to-end example flow from intent revision through deployment and reconcile run.
