# Truthwatcher v0.1.0 Readiness Audit

Date: 2026-06-25

## Executive Summary

Truthwatcher's current fundamental purpose is to prove an evidence-first, vendor-neutral path from local fixture-backed network observations to explainable network understanding. In the current repository, that means a single Go binary can collect safe fake discovery evidence, persist raw evidence, parse stored evidence into identity candidates, assets, facts, and relationships, expose graph and evidence inspection APIs, and render a small embedded UI for discovery, assets, graph, planning seeds, and deterministic questions.

The product is close to a usable v0.1.0 only if v0.1.0 is explicitly scoped as a local proof-of-concept with PostgreSQL, seeded and fixture-backed data, and no production collector promises. The main release risk is not missing ambition; it is inconsistency between the promised walkthrough/UI/docs and what a new user can actually complete without source-code knowledge.

## Audited Sources

This audit reviewed the public docs, steering docs, prompt pack, CLI implementation, API routing, embedded UI, fixture and seed flows, identity review, audit persistence, graph/evidence inspection, and help/about surfaces.

Key source areas:

- `README.md`, `ROADMAP.md`, `CONTEXT.md`, release/governance docs, and `docs/**`.
- `steering-docs/**` and `prompts/*.md`.
- `cmd/truthwatcher/main.go` and `internal/app/app.go`.
- `internal/api/**`, `internal/discovery/**`, `internal/evidence/**`, `internal/parser/**`, `internal/assets/**`, `internal/graph/**`, `internal/audit/**`, and `internal/seeding/**`.
- `web/index.html`, `web/assets/app.js`, and `web/assets/app.css`.
- `examples/fixtures/**`, `scripts/seed_demo.sql`, and `scripts/unseed_demo.sql`.

## 1. Current Fundamental Purpose

Truthwatcher currently exists to make network understanding defensible by preserving raw evidence before deriving identity, assets, facts, relationships, graph views, or answers. Its useful v0.1.0 purpose should be stated as:

> Truthwatcher is a local evidence-first network understanding app that demonstrates how fixture-backed or user-seeded data can become explainable assets, facts, relationships, and graph context while preserving provenance, confidence, uncertainty, and human review boundaries.

Important boundaries:

- It is not yet a production source of truth.
- It is not yet a network automation platform.
- It is not yet a production discovery collector.
- It is not yet an external AI/LLM product.
- It is not yet a cloud, container, or microservice deployment.

## 2. User Workflow That Proves The Purpose Today

The smallest workflow that proves the purpose today is:

1. Build the single binary.
2. Create a local PostgreSQL database and run embedded migrations.
3. Run fake fixture-backed discovery, such as `fixture://junos-mx`.
4. Inspect discovery runs and raw evidence through the API or UI.
5. Parse the stored discovery run with the matching sample parser.
6. Inspect resulting assets, facts, relationships, identity candidates, and graph context.
7. Open the embedded UI to browse discovery runs, assets, evidence drawers, graph neighborhoods, seeded context, safe discovery plans, and deterministic read-only questions.

This proves the purpose because the user can move from local raw fixture output to evidence-linked modeled knowledge without touching a network device.

## 3. Final v0.1.0 Validation Status

### Release blockers

No release-blocking issues remain in the currently validated local fixture-backed scope. The previous blocker list has been resolved by the v0.1.0 stabilization pass:

1. **Authoritative quickstart exists.** `docs/v0.1.0-quickstart.md` provides copy-paste build, database, migration, fake discovery, parse, UI, API, and acceptance commands with expected output shapes.
2. **Discovery detail evidence is visible.** The embedded Discovery Runs detail route renders run-level evidence rows and links each record to the read-only raw evidence drawer.
3. **Identity review UI exists.** The embedded `#/identity-review` page exposes pending/all candidates, candidate details, evidence links, review actions, and the handoff report.
4. **Audit inspection exists.** `GET /api/v1/audit-records` and the embedded `#/audit` page provide read-only audit inspection for discovery execution.
5. **API docs avoid unrouted examples.** The v0.1.0 walkthrough and API documentation use implemented asset-scoped relationship and graph routes.
6. **Fixture names are normalized.** Current docs and CLI examples use `fixture://junos-mx` and `fixture://iosxr-asr`.
7. **Parser persistence is accurately described.** Documentation treats parsing as an explicit second step that persists assets, facts, relationships, parse results, and identity candidates from stored raw evidence.
8. **README and roadmap scope are bounded.** Release-facing docs describe v0.1.0 as a local, read-only, fixture-backed proof of concept rather than a production live-discovery platform.
9. **Acceptance is codified.** `make acceptance-v0.1.0` runs the evidence → parse → assets/relationships/graph/audit smoke workflow when `TRUTHWATCHER_DATABASE_URL` is configured.
10. **Questions replace the old Ask surface.** The UI uses `Questions`; the legacy `#/ask` route redirects to `#/questions`, and docs explain the page is deterministic and local with no external LLM calls.

### Non-blocking follow-up candidates

- Add screenshot-based walkthroughs for the embedded UI.
- Improve graph layout, labels, and copy-to-clipboard affordances.
- Add parser and fixture authoring guides.
- Archive completed prompt-pack files into a historical folder after the v0.1.0 tag.
- Add optional local reset/demo helpers that remain safe, explicit, and non-networked.

## 4. Prompt Disposition

### Keep for v0.1.0 stabilization

These prompts represent the implemented or directly release-relevant kernel and should remain as historical traceability or cleanup guidance:

- `00_REPO_GUARDRAILS.md`
- `01_PROJECT_SKELETON.md`
- `02_CONFIG_AND_LOGGING.md`
- `03_DATABASE_MIGRATIONS.md`
- `04_HTTP_API_FOUNDATION.md`
- `05_DISCOVERY_RUNS.md`
- `06_EVIDENCE_STORE.md`
- `07_ASSETS_FACTS_RELATIONSHIPS.md`
- `08_POLICY_ALLOWLIST.md`
- `09_DISCOVERY_PROFILE_REGISTRY.md`
- `10_FAKE_COLLECTOR.md`
- `12_READONLY_DISCOVERY_WORKFLOW.md`
- `13_PARSER_INTERFACE.md`
- `14_FIRST_VENDOR_PARSERS.md`
- `15_GRAPH_QUERY_SERVICE.md`
- `16_API_DISCOVERY_ENDPOINTS.md`
- `17_API_ASSET_EVIDENCE_ENDPOINTS.md`
- `18_CONFIDENCE_AND_UNKNOWN_STATE.md`
- `19_EMBEDDED_WEB_UI_FOUNDATION.md`
- `20_UI_DISCOVERY_RUNS.md`
- `21_UI_ASSET_GRAPH.md`
- `22_UI_EVIDENCE_DRAWER.md`
- `30_AUDIT_AND_SECURITY_REVIEW.md`
- `31_TESTING_STRATEGY.md`
- `32_RELEASE_PACKAGING.md`
- `34_PARSER_PERSISTENCE_WORKFLOW.md`
- `35_IDENTITY_MERGE_AND_CONFLICT_REVIEW.md`
- `36_ASSET_EVIDENCE_UI.md`
- `37_DISCOVERY_PLAN_REVIEW_UI.md`
- `38_ARCHITECTURE_SEEDING_UI.md`
- `39_FILE_IMPORT_EXPORT_CLI.md`
- `40_AUDIT_PERSISTENCE.md`

### Rewrite before future use

These should be rewritten so they explicitly target local fixture/seeded v0.1.0 semantics or clearly say future-phase:

- `23_AGENT_WORKSPACE_FOUNDATION.md` and `24_AGENT_QUESTION_ANSWERING_OVER_GRAPH.md`: rename the feature as deterministic read-only questions, not an agentic layer.
- `25_AGENT_DISCOVERY_PLANNER.md`: frame as plan generation and review only; no execution autonomy.
- `26_ARCHITECTURE_SEEDING_WIZARD.md`: keep seed semantics but ensure UI/docs never imply seeded hints are observed proof.
- `27_PLUGIN_CONTRACTS.md`, `28_IMPORT_EXPORT_CONNECTORS.md`, and `29_BRING_YOUR_OWN_SCRIPT.md`: narrow to local dry-run/import-export boundaries; avoid plugin ecosystem promises for v0.1.0.
- `33_DOCUMENTATION_SITE.md`: rewrite as docs cleanup and quickstart completeness, not a full site requirement.
- `41_CREDENTIAL_REFERENCE_MODEL.md`: rewrite as future design only; credentials should not be necessary for v0.1.0.

### Defer until after v0.1.0

- `11_SSH_COLLECTOR_INTERFACE.md`
- `42_NETCONF_COLLECTOR_BOUNDARY.md`
- `43_SNMP_COLLECTOR_BOUNDARY.md`
- `44_GNMI_RESTCONF_COLLECTOR_BOUNDARIES.md`
- `45_ACCESS_PATH_MODEL.md`
- `46_IPAM_IMPORT_ADAPTER.md`
- `47_EMS_INVENTORY_IMPORT_ADAPTER.md`
- `48_NAUTOBOT_NETBOX_EXPORT_DRY_RUN.md`
- `49_LOCAL_AUTH_FOUNDATION.md`
- `50_SERVICE_MODEL_FOUNDATION.md`
- `51_MOP_AND_CONFIG_PLANNING_DESIGN.md`
- `52_EXTERNAL_LLM_BOUNDARY.md`
- `99_FUTURE_PHASES_NOT_NOW.md`

### Delete or archive

Do not delete prompts before v0.1.0 unless references are updated. After v0.1.0, archive completed prompts under a historical prompt-pack folder and keep only active prompts in the root `prompts/` sequence.

## 5. What v0.1.0 Should Explicitly Include

v0.1.0 should include only the smallest coherent local proof:

- Single Go binary.
- Embedded migrations.
- PostgreSQL as the only required database.
- Local CLI for `version`, `server`, `migrate`, `discover fake`, `parse discovery-run`, `devices add/list`, `export json`, `import json`, and `dev check-knowledge`.
- Fake fixture-backed discovery for included Junos and IOS-XR fixtures.
- Raw evidence persistence with metadata and hashes.
- Parser persistence from stored evidence into assets, facts, relationships, parse results, and identity candidates.
- Identity review API with non-destructive decisions and aliases where applicable.
- Discovery audit persistence.
- Asset/fact/relationship/evidence/graph read APIs.
- Embedded UI for dashboard, discovery runs, assets, evidence drawer, graph, plans, seeds, deterministic questions, and about/help.
- Seeded architecture context clearly labeled as user-seeded planning context, not proof.
- Local JSON export and validate-only import.
- A v0.1.0 walkthrough with exact commands, expected output snippets, and limitations.

## 6. What v0.1.0 Should Explicitly Exclude

v0.1.0 should explicitly exclude:

- Network writes, config changes, remediation, commits, reboots, clears, or arbitrary command execution.
- Production SSH/NETCONF/SNMP/gNMI/RESTCONF collector claims.
- Cloud services, containers, microservices, message brokers, Kubernetes, hosted modes, or external services.
- External LLM calls.
- Broad observability, telemetry stacks, tracing systems, or metrics platforms.
- Production authentication and authorization unless already implemented as a small local-only boundary.
- Production integration adapters for IPAM, EMS, Nautobot, NetBox, monitoring, cloud inventory, or source-of-truth systems.
- Continuous discovery scheduling.
- Destructive identity merge or automatic canonical rewrites.
- Raw evidence export by default.

## 7. Docs Needed For A New User

### Required before release

1. **`docs/v0.1.0-quickstart.md` or equivalent README section** with exact database, migration, fake discovery, parse, UI, and API commands.
2. **Expected output appendix** for the fixture-backed POC showing representative discovery run, evidence, asset, fact, relationship, graph, identity candidate, and audit records.
3. **Known limitations page** that states fixture-backed data is acceptable, production collectors are excluded, auth is absent, and seeded context is not proof.
4. **Identity review guide** explaining candidates, strengths, review states, non-destructive accept/reject/defer/more-evidence actions, and aliases.
5. **Audit guide** explaining what is persisted today and how to inspect it after the audit API/UI exists.
6. **UI guide** mapping each route to the evidence-first workflow.
7. **Prompt-pack status page** marking prompts as completed, v0.1.0 cleanup, deferred, rewritten, or archived.

### Useful but not required

- Screenshot-based walkthrough.
- Parser authoring guide.
- Fixture authoring guide.
- Release notes with a copy-paste acceptance test transcript.

## 8. UI Pages Needing Cleanup Before Release

### Release-blocking UI cleanup

- **Discovery Runs detail:** render evidence records, command/API names, hashes, and raw evidence drawer links.
- **Identity Review:** add a page for pending candidates, all candidates, detail, evidence link, action form, and handoff report.
- **Audit:** add a read-only audit inspection page after adding a minimal API route.
- **About:** make it the release help surface with exact v0.1.0 scope, demo commands, safety boundaries, and links to OpenAPI/docs.
- **Graph:** make empty states explain whether the user needs to parse a discovery run first.

### Nice-to-have UI cleanup

- Rename “Ask Truthwatcher” to “Questions” or “Read-only Answers” to avoid AI/LLM implications.
- Add copy buttons for IDs and API examples.
- Add a route-level “what this page proves” note.
- Add fixture/demo reset instructions.

## 9. Final Release Validation Findings

1. The README, quickstart, known limitations, audit, identity review, API, release, and roadmap docs describe the same v0.1.0 boundary: local PostgreSQL, single binary, fixture-backed discovery, explicit parsing, evidence-linked model data, read-only graph/audit/identity review inspection, and no production live discovery or write automation.
2. Quickstart commands match implemented CLI/API routes for build, migration, fake discovery, parse, server startup, UI routes, API smoke checks, and acceptance.
3. Known limitations are visible from the README and quickstart and explicitly exclude authentication/RBAC, live production collectors, credential handling, write automation, external integrations, and external LLM calls.
4. UI navigation names match release docs: Dashboard, Discovery Runs, Assets, Identity Review, Audit, Plans, Seeds, Graph, Questions, and About.
5. The acceptance script validates the v0.1.0 evidence → parse → assets/relationships/graph/audit workflow against a configured PostgreSQL database.
6. The release workflow accepts a stable `v0.1.0` semantic tag and injects version, commit, and build-date metadata into release binaries.
7. OpenAPI generation is wired into `make build`, and the current generated docs include the v0.1.0 API routes.
8. No release-facing docs imply that production live discovery, external LLM use, write automation, enterprise RBAC, containers, microservices, or external integrations exist in v0.1.0.
9. Stale references to “Ask Truthwatcher” have been reduced to intentional legacy-route handling or historical audit notes; user-facing navigation and v0.1.0 docs use “Questions.”

## 10. Suggested Post-v0.1.0 Plan

### v0.1.1 follow-up candidates

1. Add screenshot-based quickstart validation for the embedded UI.
2. Improve graph visual density, labels, and relationship filtering.
3. Add parser authoring and fixture authoring documentation.
4. Add safe local demo reset helpers that do not touch live networks.
5. Archive completed prompt-pack files and keep active future prompts separate.

### Deferred beyond v0.1.1 unless explicitly rescoped

- Production collectors and credential handling.
- Authentication, organizations, enterprise RBAC, or multi-user administration.
- External LLM integrations.
- Write automation, remediation, or network mutation.
- Containers, microservices, hosted services, or observability systems.

## Release Readiness Verdict

Truthwatcher is **ready with minor docs follow-ups** for v0.1.0 as a local fixture-backed evidence-to-graph proof of concept. No release-blocking implementation or documentation mismatch was found in the validated scope. The remaining items are documentation polish and future authoring guides, not blockers for the v0.1.0 tag.

The recommended v0.1.0 label is:

> **Truthwatcher v0.1.0: local fixture-backed evidence-to-graph proof of concept.**

Suggested release tag: `v0.1.0`.
