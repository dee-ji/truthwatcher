# Architecture Overview

Truthwatcher uses a monorepo with domain-focused packages and service entrypoints. Intent moves through validation, compilation, rollout planning, execution, and reconciliation.
